# Google OAuth client credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback

# Session signing secret (use a long random string in production)
SESSION_SECRET=dev-only-secret

# App base URL (used for redirects and links)
APP_BASE_URL=http://localhost:3000

# Optional: override the SQLite database path
# SQLITE_DB_PATH=./db/data.sqlite3

# Optional: adjust logging verbosity (debug, info, warn, error)
# LOG_LEVEL=info
